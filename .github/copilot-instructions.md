# Copilot Instructions

## Project Guidelines
- Never add or generate EF Core migrations in the KaryaCore solution. The user manages database migrations themselves; do not run `dotnet ef migrations add` or commit migration files.